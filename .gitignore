__pycache__/
.ipynb_checkpoints
.DS_Store
GuPPy/.DS_Store
z-score_methods.tgn
GuPPy/runFiberPhotometryAnalysis.ipynb
.vscode/
*.egg-info/
.clinerules/
.venv/

testing_data/
# GuPPy writes its results into the session folders it reads, and everything under
# stubbed_testing_data/ is LFS-tracked, so a stray output becomes billed LFS storage.
# Directory-only, so TDT tanks keep shipping their own StoresListing_output_<n>.txt.
*_output_*/
GuPPyParamtersUsed.json

CLAUDE.md
.codex

docs/_build/
uv.lock

tmp/

# Coverage data files (pytest-cov)
.coverage
.coverage.*
coverage.xml
