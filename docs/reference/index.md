# Reference

The reference section is for lookup material rather than narrative reading. It covers every input parameter the GuPPy GUI exposes and the canonical vocabulary GuPPy uses for its data entities.

```{toctree}
:maxdepth: 1

parameters
glossary
```
