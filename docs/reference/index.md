# Reference

The reference section is for lookup material rather than narrative reading. It covers every input parameter the GuPPy GUI exposes, every file GuPPy writes to disk, and the canonical vocabulary GuPPy uses for its data entities.

```{toctree}
:maxdepth: 1

parameters
outputs
glossary
```
