version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.12"
  jobs:
    install:
      - pip install -U pip
      - pip install .
      - pip install --group docs

sphinx:
  configuration: docs/conf.py
